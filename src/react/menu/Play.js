import React, { Component } from 'react';

class Play extends Component {

	render(){
		const {ui, domain} = this.props;
		
		return(
		<div className="Play">Play Menu</div>);
	}
}

export default Play;